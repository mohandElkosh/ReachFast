<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/gray1" /> <!-- Background color, can be transparent if not needed -->
    <stroke
        android:width="2dp"
        android:color="@color/blue" /> <!-- Border color and width -->
    <corners android:radius="8dp"/> <!-- Rounded corners if needed -->
</shape>
